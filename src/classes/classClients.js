export class Client {
    /**
     * Add a New Product 
     * @param {Object} client A new product Object
     */
    addClient(client) {
     
    }
  
    resetForm() {
      document.getElementById("client-form").reset();
    }
  
 
  }
  